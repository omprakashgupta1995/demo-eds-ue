// import loadEmbed from '../video/video.js';
import swiperFirst from '../custom-swiper/custom-swiper.js';

export default function decorate(block) {
  swiperFirst(block);
//     const swiperSecond = block.children;
//    Array.from(swiperSecond).forEach((ele)=>{
//     const hideElement = ele.children;
//   const hide = hideElement[0];
//   hide.style.display = "none";
//    })
}
