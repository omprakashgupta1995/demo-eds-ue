import {
  div,
  h1,
  p,
  input,
  label,
  span,
  button,
} from "../../scripts/domhelper.js";

export default async function decorate(block) {


    const retCal = ({class : 'main-container'},

    );


    block.append(retCal);
}